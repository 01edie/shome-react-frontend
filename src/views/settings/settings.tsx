import React from "react";
import ViewWrapper from "../../components/page/view-wrapper";

type Props = {};

function Settings({}: Props) {
  return <div>Settings</div>;
}

export default ViewWrapper(Settings, "Settings");
